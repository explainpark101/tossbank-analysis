version: '3.8'

services:
  tossbank-analyzer:
    build: .
    ports:
      - "25564:25564"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./static:/app/static
    restart: unless-stopped
    container_name: tossbank-analyzer
